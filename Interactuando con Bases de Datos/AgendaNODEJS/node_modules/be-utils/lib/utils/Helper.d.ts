export declare namespace Helper {
    let fileExists: (path: string) => boolean;
    let regexReplaceInFile: (filePath: string, pattern: RegExp, replacement: string, done?: (processed: boolean) => void) => void;
    let unsetEmptySubObjects: (obj: Object) => Object;
}
